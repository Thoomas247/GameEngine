#include "precompiled.h"

/*


void ShaderAsset::Activate()
{
	glUseProgram(m_GLID);
}

void ShaderAsset::Delete()
{
	glDeleteProgram(m_GLID);
}

void ShaderAsset::SetBool(const std::string& uniform, const bool& value)
{
	glUniform1i(glGetUniformLocation(m_GLID, uniform.c_str()), (int)value);
}

void ShaderAsset::SetInt(const std::string& uniform, const int& value)
{
	glUniform1i(glGetUniformLocation(m_GLID, uniform.c_str()), value);
}

void ShaderAsset::SetFloat(const std::string& uniform, const float& value)
{
	glUniform1f(glGetUniformLocation(m_GLID, uniform.c_str()), value);
}

void ShaderAsset::SetVec2(const std::string& uniform, const glm::vec2& value)
{
	glUniform2fv(glGetUniformLocation(m_GLID, uniform.c_str()), 1, &value[0]);
}

void ShaderAsset::SetVec3(const std::string& uniform, const glm::vec3& value)
{
	glUniform3fv(glGetUniformLocation(m_GLID, uniform.c_str()), 1, &value[0]);
}

void ShaderAsset::SetVec4(const std::string& uniform, const glm::vec4& value)
{
	glUniform4fv(glGetUniformLocation(m_GLID, uniform.c_str()), 1, &value[0]);
}

void ShaderAsset::SetMat2(const std::string& uniform, const glm::mat2& mat)
{
	glUniformMatrix2fv(glGetUniformLocation(m_GLID, uniform.c_str()), 1, GL_FALSE, &mat[0][0]);
}

void ShaderAsset::SetMat3(const std::string& uniform, const glm::mat3& mat)
{
	glUniformMatrix3fv(glGetUniformLocation(m_GLID, uniform.c_str()), 1, GL_FALSE, &mat[0][0]);
}

void ShaderAsset::SetMat4(const std::string& uniform, const glm::mat4& mat)
{
	glUniformMatrix4fv(glGetUniformLocation(m_GLID, uniform.c_str()), 1, GL_FALSE, &mat[0][0]);
}
*/